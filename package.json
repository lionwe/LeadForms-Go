{
  "name": "leadforms-go",
  "version": "1.3.5",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "release": "npm run build && powershell -NoProfile -ExecutionPolicy Bypass -File scripts/release.ps1"
  }
}
